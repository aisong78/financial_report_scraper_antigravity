# 开发日志 (Development Log)

> **文档目标**: 详细记录每次开发的功能实现、需求调整、bug 修复和遇到的问题

---

## 2025-11-23 (周六) - v1.0.0 完成

### 📊 今日开发概览
- **开发时长**: ~8 小时
- **新增代码**: ~1500 行
- **新增文件**: 5 个（pdf_downloader.py, pdf_parser.py, validator.py 等）
- **修复 Bug**: 6 个
- **新增功能**: 4 个

---

### ✅ 实现的功能

#### 1. PDF 下载器 (`pdf_downloader.py`)
**需求来源**: 用户希望能下载财报原文，不仅仅是数字数据  
**实现内容**:
- 支持 A 股（巨潮资讯）、港股（巨潮资讯）、美股（SEC Edgar）
- 自动识别股票代码类型（6 位=A股, 5 位=港股, 字母=美股）
- 下载完成后自动记录文件信息到数据库
- ` _extract_period_from_title` 方法从财报标题提取报告期
- `_extract_type_from_title` 方法从标题提取报告类型（Q1/S1/Q3/A）

**技术细节**:
```python
# 核心逻辑
def download(stock_code):
    stock_type = self._get_stock_type(stock_code)
    if stock_type in ['A', 'HK']:
        self._download_cninfo(...)  # 巨潮资讯
    elif stock_type == 'US':
        self._download_sec(...)     # SEC Edgar
```

**遇到的问题**:
- 问题 1: 巨潮资讯需要 `orgId` 才能精准查询  
  - 解决: 新增 `_get_cninfo_org_id` 方法，先调用搜索接口获取 orgId
- 问题 2: 半年报标题包含"年"字，被误判为年报  
  - 解决: 调整判断顺序，优先匹配"第三季度"、"半年"等关键词

---

#### 2. PDF 解析器 (`pdf_parser.py`)
**需求来源**: 下载的 PDF 需要转换为 TXT 才能用于数据验证和 LLM 分析  
**实现内容**:
- 使用 PyMuPDF (fitz) 提取 PDF 文本
- 支持单文件解析和批量目录解析
- 自动检测文件更新状态（如果 TXT 比 PDF 新，跳过解析）

**核心代码**:
```python
with fitz.open(pdf_path) as doc:
    for page in doc:
        text_content.append(page.get_text())
```

**性能数据**:
- 100 页 PDF: ~2 秒
- 200 页 PDF: ~4 秒

---

#### 3. 数据验证器 (`validator.py`)
**需求来源**: 用户提出"数据准确性比速度更重要"，希望实时验证  
**实现内容**:
- 从 TXT 文件中用正则表达式提取关键财务数字
- 与 AkShare 数据对比（营业收入、净利润、总资产、股东权益）
- 容差设置: 2%（考虑四舍五入误差）
- 验证结果写入 `dataquality` 字段（VERIFIED/CONFLICT）

**关键字段验证**:
```python
CRITICAL_FIELDS = {
    'revenue': (['营业收入', '营业总收入'], 1e8),
    'net_income_parent': (['归属于母公司.*净利润'], 1e8),
    'total_assets': (['资产总计', '总资产'], 1e8),
    'total_equity': (['股东权益合计'], 1e8),
}
```

**遇到的问题**:
- 问题: PDF 中的数字单位不统一（有的是"元"，有的是"亿元"）  
  - 解决: 增加单位自动识别逻辑（如果数字 >1000000 认为是元，<10000 认为是亿）

---

#### 4. 数据库增强
**需求来源**: 需要记录文件路径和验证状态  
**实现内容**:
- 新增 `financial_reports_files` 表，字段包括：
  - `file_path`: PDF 路径
  - `txt_path`: TXT 路径
  - `parse_status`: 解析状态（PENDING/SUCCESS/FAILED）
  - `download_date`: 下载时间
  - `file_size`: 文件大小
- 在 `financial_reports_raw` 表增加 `data_quality` 字段

**SQL 示例**:
```sql
CREATE TABLE financial_reports_files (
    id INTEGER PRIMARY KEY,
    stock_code TEXT,
    report_period TEXT,
    report_type TEXT,
    file_path TEXT,
    txt_path TEXT,
    download_date TEXT,
    file_size INTEGER,
    parse_status TEXT DEFAULT 'PENDING',
    UNIQUE(stock_code, report_period, report_type)
);
```

---

### 🔧 需求调整

#### 调整 1: 从"离线验证"改为"实时验证"
**原需求**: 下载所有数据后再统一验证  
**调整后**: 下载一个文件就立即验证一个  
**原因**: 用户提出"都开始分析了才发现数据有误，之前的工作就白做了"  
**影响**: 下载流程稍慢（每个文件多 0.5 秒验证时间），但数据质量有保障

---

#### 调整 2: 筛选器从侧边栏移到主界面
**原需求**: 筛选器放在侧边栏  
**调整后**: 筛选器放在主界面表格正上方  
**原因**: 用户反馈"看不到筛选功能在哪里"  
**实现**: 使用 `st.columns` 布局，左侧放筛选器，右侧留空

---

#### 调整 3: 横轴时间格式从日期改为"2023A"
**原需求**: 表格横轴显示 "2023-12-31"  
**调整后**: 显示 "2023A"  
**原因**: 用户反馈"希望时间维度是财报类型，而不是具体日期"  
**实现**: 新增 `generate_report_name` 函数，拼接年份和类型

---

### 🐛 修复的 Bug

#### Bug 1: 筛选后表格消失
**现象**: 切换筛选器后，表格数据全部消失  
**原因**: Streamlit 页面刷新时，`if st.button("加载数据")` 的状态被重置为 False  
**修复**: 引入 `st.session_state` 存储数据，页面刷新时从 session 读取  
**影响范围**: `app.py` 核心逻辑  
**修复代码**:
```python
# 初始化 session_state
if 'df_raw' not in st.session_state:
    st.session_state.df_raw = pd.DataFrame()

# 数据展示逻辑
if not st.session_state.df_raw.empty:
    df_raw = st.session_state.df_raw
    # ...
```

---

#### Bug 2: `df_derived` 表缺少 `report_type` 字段
**现象**: 筛选时报错 `KeyError: 'report_type'`  
**原因**: `financial_indicators_derived` 表设计时没有 `report_type` 字段  
**修复**: 在 `app.py` 中用 `pd.merge` 把 `df_raw` 的 `report_type` 合并到 `df_derived`  
**修复代码**:
```python
if 'report_type' not in df_derived.columns:
    temp_map = df_raw[['report_period', 'report_type']].drop_duplicates()
    df_derived = pd.merge(df_derived, temp_map, on='report_period', how='left')
```

---

#### Bug 3: 缩进错误 (`IndentationError`)
**现象**: `app.py` 第 215 行报缩进错误  
**原因**: 多次修改代码时，`with col_filter:` 块的缩进被打乱  
**修复**: 手动调整缩进，确保与 `if` 块对齐  
**教训**: 使用 IDE 的自动格式化功能，避免手动调整缩进

---

#### Bug 4: 半年报被误判为年报
**现象**: "2025年半年度报告" 的 `report_period` 被识别为 `2025-12-31`  
**原因**: 标题中包含"年"字，先被 `if '年度报告' in title` 匹配  
**修复**: 调整判断顺序，优先匹配季度和半年关键词  
**修复前**:
```python
if '年度报告' in title:
    return 'A'
elif '半年' in title:
    return 'S1'
```
**修复后**:
```python
if '半年' in title:
    return 'S1'
elif '年度报告' in title:
    return 'A'
```

---

#### Bug 5: AkShare 返回未来日期
**现象**: 数据库里出现 "2025-09-30" 这种未来日期  
**原因**: AkShare 的 `report_period` 有时是财报截止日，有时是公告日  
**临时方案**: 暂时不修复，在界面上标注（后续可增加日期校验逻辑）  
**长期方案**: 用 PDF 里的日期进行校正

---

#### Bug 6: `receivables_turnover_days` 显示为英文
**现象**: 界面上部分字段依然是英文  
**原因**: `field_map` 字典遗漏了这个字段  
**修复**: 在 `field_map` 中补全  
```python
'receivables_turnover_days': '应收账款周转天数 [天]',
```

---

### 🤔 发现的问题（待解决）

#### 问题 1: PDF 解析准确度未验证
**描述**: 目前只是简单的文本提取，未测试复杂表格的识别准确度  
**影响**: 可能导致数据验证误报（明明一致，但因为解析错误显示冲突）  
**计划**: v1.1 增加 LLM 辅助验证（让 AI 理解上下文）

---

#### 问题 2: 单元测试缺失
**描述**: 所有模块都没有单元测试  
**影响**: 重构时容易引入 bug  
**计划**: v1.1 为核心模块添加单元测试（calculator, validator）

---

#### 问题 3: 异常处理不完善
**描述**: 网络超时、文件损坏等异常未充分处理  
**影响**: 用户体验差（程序直接崩溃）  
**计划**: 增加 try-catch 和重试机制

---

### 📈 性能数据

#### 数据抓取性能
- 容百科技（688005）15 年数据: ~10 秒
  - AkShare 抓取: 2 秒
  - 指标计算: 1 秒
  - PDF 下载: ~5 秒（5 个文件）
  - PDF 解析: ~2 秒

#### 数据验证性能
- 单个财报验证: ~0.5 秒
  - 数据库查询: 0.05 秒
  - TXT 文件读取: 0.1 秒
  - 正则匹配: 0.3 秒
  - 数据库更新: 0.05 秒

---

### 💡 技术亮点

#### 1. 策略模式实现多市场支持
不同市场使用不同的下载策略，但对外接口统一：
```python
downloader.download("688005")  # A股
downloader.download("00700")   # 港股
downloader.download("AAPL")    # 美股
```

#### 2. 正则表达式动态匹配
支持多种财报格式的关键字：
```python
pattern = r'营业收入[^\d]{0,20}?([\d,]+\.?\d*)'
```

#### 3. Streamlit 状态管理
使用 `session_state` 避免数据丢失：
```python
st.session_state.df_raw = df
```

---

### 🎓 经验总结

#### 1. 用户反馈比预设计更重要
- **案例**: 原计划是"先收集数据，后期再验证"，但用户提出要"实时验证"
- **教训**: 快速 MVP，快速迭代，不要闭门造车

#### 2. 错误处理要从一开始就考虑
- **案例**: 多次因为缺少 try-catch 导致程序崩溃
- **教训**: 写代码时就加 try-catch，而不是出问题再补

#### 3. 数据库设计要预留扩展空间
- **案例**: 一开始没设计 `financial_reports_files` 表，后来补加很麻烦
- **教训**: 设计表结构时多想一步："未来可能需要什么？"

---

### 📅 明日计划

1. **功能开发**:
   - [ ] Streamlit 界面增加"查看原文"按钮
   - [ ] 显示数据质量标识（✅ VERIFIED / ⚠️ UNVERIFIED）

2. **代码优化**:
   - [ ] 增加日志系统（替代 print）
   - [ ] 完善异常处理
   - [ ] 为 validator.py 增加单元测试

3. **文档更新**:
   - [ ] 更新 README.md
   - [ ] 提交代码到 GitHub

---

## 2025-11-22 (周五) - 项目启动

### ✅ 今日完成
- 数据库表结构设计（4 张表）
- data_fetcher.py 初版（A 股数据抓取）
- calculator.py 初版（15+ 指标计算）
- Streamlit 基础界面

### 🐛 发现的问题
- AkShare 有时返回未来日期（如 2025-09-30，但现在是 2025-11-23）
- 数据转置逻辑有误（最初使用了错误的 transpose）

---

## 2025-11-21 (周四) - 需求调研

### 📋 用户访谈记录
- 用户是个人投资者，希望有免费的财务数据分析工具
- 不想付费使用 Wind
- 希望数据可靠，宁可慢一点也要准确

### 🎯 核心需求
1. 支持 A/港/美三地市场
2. 数据要能验证（不能出错）
3. 界面要简洁（全中文）

---

## 模板说明

每日开发记录建议包含以下部分：

```markdown
## YYYY-MM-DD (星期X) - 版本/里程碑

### 📊 今日概览
- 开发时长
- 新增代码量
- 功能数量

### ✅ 实现的功能
- 功能 1
  - 需求来源
  - 实现内容
  - 技术细节
  - 遇到的问题

### 🔧 需求调整
- 调整 1
  - 原需求
  - 调整后
  - 原因
  - 影响

### 🐛 修复的 Bug
- Bug 1
  - 现象
  - 原因
  - 修复方法
  - 教训

### 🤔 发现的问题（待解决）
- 问题 1
  - 描述
  - 影响
  - 计划

### 📈 性能数据
- 性能指标记录

### 💡 技术亮点
- 值得记录的技术实现

### 🎓 经验总结
- 今日学到的经验

### 📅 明日计划
- 待办事项
```
